nav.navbar.navbar-expand-lg.bg-light
  .container-fluid
    a.navbar-brand(href='#') Gimnasio
    button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarSupportedContent' aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation')
      span.navbar-toggler-icon
    .collapse.navbar-collapse#navbarSupportedContent
      ul.navbar-nav.me-auto.mb-2.mb-lg-0        
        li.nav-item
          a.nav-link(href='/') Inicio

        if user
          if user.tipo === 'ADMIN'
            li.nav-item
              a.nav-link(href='/Cliente') Clientes
            li.nav-item
              a.nav-link(href='/Entrenador') Entrenadores
            li.nav-item
              a.nav-link(href='/admin') Panel de Administracion

          if user.tipo === 'ENTRENADOR'
           li.nav-item
              a.nav-link(href='/Entrenador') Entrenadores
           li.nav-item
              a.nav-link(href='/sesion') Sesiones

          if user.tipo === 'CLIENTE'
            li.nav-item
              a.nav-link(href='/Plan_Membresia') Planes de Membresía
            li.nav-item
              a.nav-link(href='/ClientePlan') Mis Planes
            li.nav-item
              a.nav-link(href='/Cliente/mi-perfil') Mi Perfil
          li.nav-item
            a.nav-link(href='/auth/logout') Cerrar sesión
        else
          li.nav-item
            a.nav-link(href='/auth/login') Iniciar sesión
